name: black-box-lm
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip>=23
  - numpy>=1.24
  - pandas>=2.0
  - matplotlib>=3.7
  - scikit-learn>=1.3
  - pyyaml>=6.0
  - tqdm>=4.66
  - jupyter>=1.0
  - ipykernel>=6.0
  - pytest>=7.4
  - pip:
      - torch>=2.1
      # transformers must stay below 5.x: the GPT-NeoX rename there breaks the
      # TransformerLens Pythia weight conversion (see requirements.txt).
      - transformers>=4.40,<5
      - transformer_lens>=2.0,<3
