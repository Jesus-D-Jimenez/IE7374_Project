# Core interpretability + editing stack.
# transformer_lens 2.x is pinned deliberately: its 3.x line requires transformers >= 5,
# whose GPT-NeoX rename (embed_out -> lm_head) breaks the Pythia weight conversion.
torch>=2.1
transformers>=4.40,<5
transformer_lens>=2.0,<3

# Data, analysis, and plotting
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3

# Config and utilities
pyyaml>=6.0
tqdm>=4.66

# Notebooks
jupyter>=1.0
ipykernel>=6.0

# Testing
pytest>=7.4
